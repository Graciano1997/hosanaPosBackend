# PostgreSQL. Versions 9.3 and up are supported.
#

default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see Rails configuration guide
  # https://guides.rubyonrails.org/configuring.html#database-pooling
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: hozsystem_development
  username: postgres
  password: gra

test:
  <<: *default
  database: hozsystem_test
  username: postgres
  password: gra

# production:
#   <<: *default
#   database: hozsystem_production
#   username: gra
#   password: <%= ENV["BLOG_APP_DATABASE_PASSWORD"] %>